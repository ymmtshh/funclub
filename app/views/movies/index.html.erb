<%= @user.profile.name %>さんのMOVIE一覧<br>

<% @movies.each do |movie| %>
  <%= link_to user_movie_path(@user, movie) do %>
  <%= movie.title %><br>
  <% end %>
<% end %>

<% if @user.id == current_user.id %>
  <%= link_to "新規作成", new_user_movie_path(@user.id) %>
<% end %>