
<% @schedules.each do |schedule| %>
  <%= link_to schedule_path(schedule) do %>
  <%= schedule.title %><br>
  <% end %>
<% end %>


<% if @user.id == current_user.id %>
  <%= link_to "新規作成", new_user_schedule_path(@user.id) %>
<% end %>